# UNI-project 🎓

A simple Python (Streamlit) application demonstrating **temporal database concepts** using **SQL Server System-Versioned Tables**.

## 🧩 Technologies

- Python 3.10+
- Streamlit
- pyodbc
- SQL Server 2016 or later

## 🚀 How to Run

1. Create a database and temporal table in SQL Server (see `temporal.sql`).
2. Install dependencies

```bash
pip install streamlit pyodbc pandas
```

3. In terminal run: streamlit run app.py
